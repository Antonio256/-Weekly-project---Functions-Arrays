/*

*/


const main = (i) => {
 for (a=0; a<i; a++) {
   var types = [];
   if (odd(c)==true) {
     types.push("odd")
   } else {
     types.push("even")
   }
   if (prime(a)==true) {
     types.push("prime")
   }
   console.log(a, types);
 }
}
